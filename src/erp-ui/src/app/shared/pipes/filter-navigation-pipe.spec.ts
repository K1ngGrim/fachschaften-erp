import { FilterNavigationPipe } from './filter-navigation-pipe';

describe('FilterNavigationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNavigationPipe();
    expect(pipe).toBeTruthy();
  });
});
